<<<<<<< Updated upstream
# For local development (without Docker)
# DATABASE_URL=postgres://abc:abc123@localhost:5432/abc_db?sslmode=disable

# For Docker Compose (default)
DATABASE_URL=postgres://abc:abc123@localhost:5432/abc_db?sslmode=disable
=======
# PostgreSQL connection string
# For Docker Compose with external DB (VM deployment):
DATABASE_URL=postgres://bankease:bankease123@<db-host>:5432/bankease_db?sslmode=disable
# For docker-compose.local.yml (DB included in compose):
# DATABASE_URL=postgres://bankease:bankease123@db:5432/bankease_db?sslmode=disable

>>>>>>> Stashed changes
PORT=8080

# JWT secret — MUST be the exact same value as JWT_SECRET in identity-service/.env
JWT_SECRET=secret

# Azure Blob Storage (for image upload endpoint POST /api/upload/image)
# Paste the full account-level SAS URL from the Azure portal
<<<<<<< Updated upstream
AZURE_STORAGE_SAS_URL=https://abc.blob.core.windows.net
# Container must already exist in Azure Storage
=======
AZURE_STORAGE_SAS_URL=
AZURE_STORAGE_CONTAINER=images
>>>>>>> Stashed changes
AZURE_STORAGE_CONTAINER=images
